# spg-improve-loop · tick 76

📦 PROJECT: stuffprettygood.com
🎯 SUGGESTED: Lane B #13 — sitemap priority + changefreq. SEO surface bumps home→1.0/daily, money pages→0.9/weekly, categories→0.8/weekly, guides→0.7/monthly, products→0.6/monthly, legal→0.4/yearly, utility→0.3/yearly. Closes the "all routes look identical to Google" gap.
✅ DONE: scripts/build.mjs sitemap writer extended with `_spgSitemapMeta` map + `_spgSitemapMetaDefault` fallback + per-URL `<priority>`/`<changefreq>` injection (23 explicit entries, 2 prefix fallbacks for guides/* + products/*). 188/188 URLs now carry both tags. `node --check` + `node scripts/validate.mjs` + `node scripts/build.mjs` all exit 0. XML well-formedness verified (xml.etree parse, all 188 urls have all 4 children). Also: comment + false-alarm close on `t_meta_cf_creds` (already-correct docs card).
🧪 TESTED: Gate 1 `node --check scripts/build.mjs` → 0. Gate 2 `node scripts/validate.mjs` → "validation passed: 155 catalog records, 155 product pages". Gate 3 `node scripts/build.mjs` → "built 155 approved products, 10 guides". Gate 3.5 `git status -s` → 3 modified (scripts/build.mjs + dist/sitemap.xml expected, dist/feed.xml lastBuildDate ticker = build-regen side effect, separate chore commit per pitfall #84). Gate 4 `grep -n _spgSitemapMeta scripts/build.mjs` → present (line 2658). Gate 5 offline `python xml.etree.ElementTree.parse(dist/sitemap.xml)` → 188 children, all with priority + changefreq + loc + lastmod. Gate 6 distribution `grep -oE '<priority>[0-9.]+</priority>'` → 1/5/7/10/155/6/4 = 188 ✓. Changelog gate 7 `git log --oneline -1` → 4d2d581c (tick 75 RSS). No browser QA needed (sitemap.xml is bot-only).
📊 RESULTS: commit `4c5d6e7f` "feat(seo): Lane B #13 sitemap priority + changefreq …" (34 lines added to scripts/build.mjs + regenerated dist/sitemap.xml). Commit `6f0e8b1a` "chore(dist): feed.xml lastBuildDate re-gen (gate 3.5 side effect)" (1-line date ticker per pitfall #84). push: `GIT_CONFIG_SYSTEM=/dev/null GIT_TERMINAL_PROMPT=0 git push origin deploy/legal-expansion-and-signup-modal` → tracked (commit will appear on origin per pitfall #96 push recipe; verified `git ls-remote origin deploy/legal-expansion-and-signup-modal` matches `git rev-parse HEAD`).
🔗 LINKS: preview URL will appear in wrangler deploy output (next call). Live verification: `curl -A "Mozilla/5.0" "https://stuffprettygood.com/sitemap.xml?cb=$RANDOM" | python -c "import sys, xml.etree.ElementTree as ET; r=ET.parse(sys.stdin).getroot(); print('urls:', len(list(r)), 'priorities:', len(set(u.find('{http://www.sitemaps.org/schemas/sitemap/0.9}priority').text for u in r)))"`. Ticket: t_meta_cf_creds false-alarm comment posted (no new tickets filed — no real bugs surfaced).
🧠 MEMORY: CRLF line endings in scripts/build.mjs are the canonical line terminator (per pitfall #100 confirmed in this tick); all future surgical Python replacements MUST use `'\r\n'.join(lines)` not `'\n'.join(lines)`, and read with `io.open(..., newline='')` to disable Python's universal-newline translation. Lane B pattern family now has 5 entries: site-wide JSON-LD (t42), Twitter Card meta (t44), format-detection (t45), og:site_name+locale (t49), RSS feed (t75), sitemap priority+changefreq (t76). Next Lane B candidate: per-page `<link rel="alternate" type="application/rss+xml">` from every category → RSS feed (closes the RSS-surface gap so the feed is auto-discoverable from each category page).