# spg-improve-loop · tick 42

📦 PROJECT: stuffprettygood.com
🎯 SUGGESTED: Lane B — add ItemList JSON-LD to category pages so Google rich snippets can read them as a list of products
✅ DONE: categoryItemListJsonLd() helper + layout() opts.categoryItemListJsonLd wiring + category mkdirPage loop emits ItemList for 7 static-grid category routes + useful-finds emits top-24 ItemList. Walmart intentionally skipped (live Impact picks only; no static `category === 'walmart'` source).
🧪 TESTED: `node --check scripts/build.mjs` exit 0 · `node scripts/validate.mjs` exit 0 (155 catalog, 155 product pages) · `node scripts/build.mjs` rebuilt dist/ · per-page JSON-LD shape gate via inline python (under-25/under-50/travel/home-office/kitchen/pets/tech/walmart/useful-finds): walmart correctly MISSING, other 8 each carry `ItemList` with itemListElement matching numberOfItems, first item URL = `https://stuffprettygood.com/products/<id>/`. /under-50/ JSON-LD block count = 5 (Organization + WebSite + BreadcrumbList + ItemList + FAQPage).
📊 RESULTS: scripts/build.mjs +25 lines (categoryItemListJsonLd helper + opts wiring + 1 line in category loop). 8 dist pages regenerated (under-25 / under-50 / travel / home-office / kitchen / pets / tech / useful-finds). dist/sitemap.xml lastmod drift. commit 60774ca · wrangler deploy 08568669.stuffprettygood.pages.dev (Uploaded 9 files, 842 already uploaded) · push landed to origin (verified via git ls-remote == git rev-parse == 60774ca) · custom-domain CDN still serving pre-deploy HTML per pitfall #76 (4th reproduction); preview URL authoritative.
🔗 LINKS: live https://stuffprettygood.com/under-50/ · /kitchen/ · /travel/ · /home-office/ · /pets/ · /tech/ · /under-25/ · /useful-finds/ (preview URL authoritative per pitfall #33; custom-domain cache lags 1-6h per pitfall #76)
🧠 MEMORY: next-tick-Hermes — the static `category === 'walmart'` field doesn't exist in data/products.json (walmart is a merchant overlay populated at runtime by Impact live picks). Any future SEO work that needs "all walmart products" must use the live Impact endpoint, not the static catalog. The ItemList pattern is now wired and reusable for any future category that does have a static picks array (recipes, gift ideas, etc.).