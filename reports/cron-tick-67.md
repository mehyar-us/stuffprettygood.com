# spg-improve-loop · tick 67

📦 PROJECT: stuffprettygood.com
🎯 SUGGESTED: Lane A #23 — Affiliate click optimization. Money-path P1 lane. Every `/go/<id>/` link now (a) renders a sticky mobile bottom-bar CTA on the 155 product pages with thumb-reachable "View on Amazon" + product meta, AND (b) fires a `window CustomEvent('spg-affiliate-click', {detail:{id,source,ts}})` on every outbound click, ready for omni to wire the Resend post-click value ladder.
✅ DONE: Lane A #23 shipped — `node --check scripts/build.mjs` OK; `node scripts/validate.mjs` 155/155 catalog OK; `node scripts/build.mjs` 155 products + 10 guides + sitemap; `node scripts/check-inline-iife.mjs` 2419/2419 inline scripts parse cleanly across 188 dist pages. Surgical patch to `scripts/build.mjs:1326` (product template) injects `<div class="spg-sticky-cta">` block right before `</article>` + adds `data-affiliate-click data-affiliate-source="detail-inline"` to the existing inline `Get` button. Surgical patch to `scripts/build.mjs:225` (`tuneLinks` IIFE in `backToTop()`) appends `a.setAttribute("data-affiliate-click","")` + a `click` listener that dispatches the custom event with `id` (parsed from `/go/<id>/`) + `source` (from `data-affiliate-source` or `"inline"`) + `ts` (Date.now()). `src/styles.css` appended with `.spg-sticky-cta` rules (mobile-first, `position:fixed; bottom:0`, glass surface with backdrop-blur, `env(safe-area-inset-bottom)` for iPhone, slide-up animation after 1.2s with `prefers-reduced-motion` override, hidden on `>=981px`, body padding-bottom 96px on `body[data-route^="products/"]` to prevent footer overlap). Diff: scripts/build.mjs +2/-2, src/styles.css +18/-0, dist/ +535/-499 (155 product pages updated + 188 other pages touched by the tuneLinks click-tracker patch + sitemap lastmod). Commit `2211889d feat(affiliate): Lane A #23 sticky mobile CTA + click tracking on product pages` (347 files). Pushed clean via pitfall #96 recipe `GIT_CONFIG_SYSTEM=/dev/null GIT_TERMINAL_PROMPT=0 git push origin deploy/legal-expansion-and-signup-modal` (10s). Ground-truth verified: `git ls-remote origin deploy/legal-expansion-and-signup-modal | awk '{print $1}'` = `2211889de13826a1651a358f4ef9fb30c3fc599c` = `git rev-parse HEAD`, `git rev-list --count origin..HEAD` = 0. Wrangler Pages deploy landed at `366e92a3.stuffprettygood.pages.dev` (345 files / 13.98 sec).
🧪 TESTED: Live preview verification. `curl -A "Mozilla/5.0" https://366e92a3.stuffprettygood.pages.dev/products/gift-usb-c-charging-station/` returns: 1× `spg-sticky-cta`, 3× `data-affiliate-click`, 3× `data-affiliate-source`, 1× `spg-affiliate-click` (event dispatcher IIFE present). All 155 product pages scan clean: `grep -c "spg-sticky-cta"` = 1, `grep -c "data-affiliate-click"` ≥ 2 on every product page (155/155 OK). Cross-check: 0 non-product pages have `spg-sticky-cta` (correctly scoped via product template only). Custom-domain cache stale as expected (pitfall #76 chronic, 1-6h propagation); preview URL authoritative per pitfall #33.
📊 RESULTS: Commit SHA `2211889d`. CF Pages deploy `366e92a3.stuffprettygood.pages.dev`. 1 new OMNI ticket filed: `t_1f986e8b` (assignee=omni, priority=2, body has trigger event + desired conversion + Resend audience/cohort spec + acceptance criteria + curl test recipe).
🔗 LINKS: Live preview (authoritative until custom-domain cache busts): https://366e92a3.stuffprettygood.pages.dev/products/gift-usb-c-charging-station/?cb=$(date +%s) · Commit: https://github.com/mehyar-us/stuffprettygood.com/commit/2211889d · OMNI ticket: `hermes kanban --board stuffprettygood-com show t_1f986e8b` · IIFE reference: `references/lane-a-affiliate-click-tracking.md` (TODO if this pattern sticks).
🧠 MEMORY: Lane A #23 is the **3rd Lane A pattern this quarter that explicitly hands off to omni**: Lane A #9 (dark-mode — no omni), Lane A #21 (iOS Safari install hint — omni routing for VAPID if PWA install lands), Lane A #23 (affiliate click tracking — omni Resend post-click ladder). Pattern is forming: **whenever the change introduces a new client-side event (clicks, saves, page-views beyond analytics), file an OMNI: ticket in the SAME tick** that ships the event — so omni is already spinning up the Resend audience + draft schedule while the deploy propagates. This is the inverse of the "file ticket, implement later" pattern; it's "implement + file trigger in parallel." The 155-page product-template sweep was the easy win (1 line of HTML in the loop body, 1 CSS rule appended) — but the `tuneLinks` patch is what unlocks omni for site-wide `/go/` link tracking on the OTHER 33 product-context pages (home grid, category grids, AI bubble picks, guides). Verify on next tick that `t_1f986e8b` is picked up by omni within 60s of dispatcher tick (the 60s race from pitfall notes applies — read kanban list before calling dispatch). Future Lane A wins that fit the same "ship-event → file-omni-ticket" shape: (a) `signup_view` event on signup page entry → omni Resend exit-intent; (b) `ai_bubble_open` event on every AI panel launch → omni Resend weekly-picks broadcast opt-in flow; (c) `scroll_50pct` event on home → omni Resend re-engagement for non-subscribers.