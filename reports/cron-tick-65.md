# spg-improve-loop · tick 65

📦 PROJECT: stuffprettygood.com
🎯 SUGGESTED: Lane B #5 — Add `<link rel="preconnect">` + `dns-prefetch` for Microsoft Clarity (every page), internal API (every page), and Amazon ad system (product detail only). Cuts DNS+TLS handshake time on first paint by 100-300ms for the analytics tag and the signup form post.
✅ DONE: Wired `preconnectHints` into `layout()` (scripts/build.mjs:1128-1133) — derives Amazon ad-system preconnect only when `opts.route` matches `^products/`. Injected 4 site-wide link tags into the `<head>` immediately before the Microsoft Clarity inline script (scripts/build.mjs:1149-1154). 188 non-/go/ pages now ship the connectivity hints; 155 product pages additionally ship the Amazon-adsystem hint.
🧪 TESTED: `node --check scripts/build.mjs` → OK. `node scripts/validate.mjs` → 155/155 catalog OK. `node scripts/build.mjs` → 155 products, 10 guides. `node scripts/check-inline-iife.mjs` → 2419/2419 inline scripts parse cleanly across 188 dist pages. `git diff scripts/build.mjs` → +7/-1 lines, 1 derived const + 4 link tags injected in layout template. Per-route preconnect verification: `curl -A "Mozilla/5.0" https://236dd1e8.stuffprettygood.pages.dev/` returns 4 hints (clarity + API); `…/products/gift-usb-c-charging-station/` returns 5 (clarity + API + amazon-adsystem); `…/go/gift-usb-c-charging-station/` returns 0 (correct — merchant redirect pages stay hint-free).
📊 RESULTS: 189 files changed (1 source + 188 dist). Commit SHA pending (next line). CF Pages deploy `236dd1e8.stuffprettygood.pages.dev` succeeded in 14.80s (663/852 already uploaded, 189 new files). Preview URL serves the new preconnect tags correctly per curl verification above.
🔗 LINKS: Live preview: https://236dd1e8.stuffprettygood.pages.dev/?cb=$(date +%s) · Product example: https://236dd1e8.stuffprettygood.pages.dev/products/gift-usb-c-charging-station/?cb=$(date +%s) · Lane B pattern family index: refs/lane-b-html-meta-extension (Twitter Card tick 44, format-detection tick 45, og:site_name+locale tick 49, preconnect tick 65).
🧠 MEMORY: Lane B HTML-meta-extension pattern family is now 4 entries deep. The preconnect+dns-prefetch pair is the canonical "performance meta" addition; site-wide hints go in the layout template, route-scoped hints go through `opts.route`-gated derived constants (not by threading new mkdirPage params). Next Lane B candidate: `og:locale:alternate` (skip — no alternate locales), `article:author`/`article:published_time` (skip — no editorial posts), `apple-itunes-app` (skip — no native app). The HTML-meta-extension lane may be near exhaustion; future Lane B ticks should pivot to dynamic content (JSON-LD, sitemap splits, breadcrumb hierarchy) or PWA performance (service worker cache strategy) instead.