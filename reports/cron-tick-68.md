# spg-improve-loop · tick 68

📦 PROJECT: stuffprettygood.com
🎯 SUGGESTED: Lane A #24 exit-intent signup prompt — bottom-right slide-up card fires when user mouses toward address bar, opens email-only mini-form that posts to existing /api/subscribe endpoint with source=exit-intent, dispatches `spg-signup-exit-intent` CustomEvent for OMNI welcome-drip webhook.
✅ DONE: scripts/build.mjs (added exitIntentScript() IIFE + wired into layout template via ${exitIntentScript()} between ${iosInstallHintScript()} and ${modalHtml}); src/styles.css (+28 lines: .spg-exit-intent bottom-right slide-up card, .spg-exit-form, .spg-exit-btn gradient CTA, mobile + dark-mode tokens, prefers-reduced-motion guard); 188/188 non-/go/ dist pages rebuilt with the IIFE injected.
🧪 TESTED: `node --check scripts/build.mjs` → 0; `node scripts/validate.mjs` → 155 catalog records OK; `node scripts/build.mjs` → built 155 approved products + 10 guides; `node scripts/check-inline-iife.mjs` → 2607 inline scripts / 0 syntax errors; offline Python walker across 188 non-/go/ pages → all 6 markers (spg-exit-intent-shown, spg-signup-exit-intent, spg-exit-title, spg-exit-intent, spg-exit-close, COOLDOWN_MS, /api/subscribe) present, 0 bad; live preview URL https://5aee0b25.stuffprettygood.pages.dev/walmart/ `curl -A "Mozilla/5.0" | grep -oE` → /api/subscribe x3, COOLDOWN_MS x2, spg-exit-intent-shown x2, spg-signup-exit-intent x2; `browser_navigate` to https://5aee0b25.stuffprettygood.pages.dev/ → page loads with full nav + product grid; `browser_console(expression=)` → `hasIife: true` confirms script parsed and DOM contains the IIFE source.
📊 RESULTS: commit SHA pending; CF Pages deploy preview ID 5aee0b25; 192 dist files staged (`git add -f scripts/build.mjs src/styles.css dist/` per pitfall #52); OMNI ticket filed separately for the webhook wiring (this tick ships the client-side surface, not the server-side consumption).
🔗 LINKS: live preview: https://5aee0b25.stuffprettygood.pages.dev/ ; custom domain: https://stuffprettygood.com/ (preview authoritative per pitfall #76 — production HTML still serves the prior revision's content; the manifest is at chronic stale-7-key state on prod per pitfall #95); report file: `reports/cron-tick-68.md` (this file).
🧠 MEMORY: The product-side fire for exit-intent signup is live on preview. OMNI needs to consume the `spg-signup-exit-intent` CustomEvent AND wire `/api/subscribe` to write the source=exit-intent subs into a Resend audience so the welcome-drip + 1d/3d/7d ladder emails fire. Next-tick-Hermes can either (a) Lane A continue with #25 (recently-viewed products bar), (b) Lane B #6 (og:locale:alternate + article:author JSON-LD for /stories/), or (c) verify the exit-intent → Resend webhook is wired by OMNI before shipping another product-side surface. Pitfall reminder: tick 67's `spg-affiliate-click` event ALSO needs OMNI wire-up before another affiliate-tracking surface ships.
