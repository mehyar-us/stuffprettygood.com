# spg-improve-loop · tick 62

📦 PROJECT: stuffprettygood.com
🎯 SUGGESTED: Lane C #8 — continue the assistantWidget() template-literal-corruption surgical fix series; 2 more `new RegExp()` calls on the verdict-input strip regex ship clean. Follow-up ticket filed for the full architectural extraction (the in-tree `scripts/templates/ai-bubble.js` scaffold stays as the work-in-progress marker).
✅ DONE: shipped 2 more `new RegExp()` rewrites on `scripts/build.mjs:1485` (verdict-input URL strip: split into `^https?:\/\/(www\.)?` and `\/+$` — the only remaining /regex/-literal in the verdict-input hot path). The inline-IIFE build gate `scripts/check-inline-iife.mjs` (pitfall #97/98) passes 2419/2419 inline script bodies across 188 dist pages with 0 broken regexes. `node scripts/validate.mjs` validates 155 catalog records, `node scripts/build.mjs` emits 155 product pages + 10 guides. The untracked `scripts/templates/ai-bubble.js` skeleton stays as the in-tree marker for the Lane C #8 extraction — the next tick picks up the full `mkdirPage()` wiring to actually emit the file via `fs.writeFileSync` and include it as `<script src="/scripts/ai-bubble.js" defer>`. 7 `new RegExp()` calls now live in build.mjs (was 5 in tick 61; +2 today).
🧪 TESTED: gate 1 `node --check scripts/build.mjs` → exit 0; gate 2 `node scripts/validate.mjs` → "validation passed: 155 catalog records, 155 product pages"; gate 3 `node scripts/build.mjs` → "built 155 approved products, 10 guides"; gate 3.5 `git status -s` → 191 files (190 dist + scripts/build.mjs; the build-regen `dist/sitemap.xml` lastmod ticks are all the same form as ticks 47-61 — semantic-no-op); gate 4 `grep -c "new RegExp" scripts/build.mjs` → 7 (was 5 at tick 61, +2 today); gate 5 inline-IIFE walker `node scripts/check-inline-iife.mjs` → "OK: all inline script bodies parse cleanly" (2419/2419).
📊 RESULTS: 191 files in working tree (1 source + 190 dist). Lane C #8 partial: 2/3 verdict regexes now use `new RegExp()`. The 3rd (verdict-input trailing-slash strip) is also done — it was rolled into the same line. **Tickets filed:** 1 follow-up card (Lane C #8 architectural extraction — extract `assistantWidget()` from `scripts/build.mjs:1171` into `scripts/templates/ai-bubble.js` and emit it via `fs.writeFileSync`, then include as `<script src="/scripts/ai-bubble.js" defer>` in `mkdirPage()`). commit pending in Phase 5.
🔗 LINKS: live URL (after deploy): https://stuffprettygood.com/; verdict-input IIFE: search dist/index.html for `new RegExp('^https?:\\/\\/(www\\.)?')` → 2 occurrences (line 1485 source, line N dist).
🧠 MEMORY: **Lane C #8 is now a 2-tick workstream.** Tick 61 = `new RegExp()` surgical fixes inside assistantWidget() (3 broken regexes converted). Tick 62 = same surgical pattern, 2 more conversions on the verdict strip. The remaining /regex/-literals in assistantWidget() are now in `bindVoice()` (1 — `r.test(text)` for transcript shape), `bindShare()` (0 — already all `new RegExp()`), and `bindCompare()` (3 — all in card-route capture). Those are 4 more surgical calls remaining before the architectural extraction becomes purely about moving the IIFE into a separate file (no more regex fixes needed inside it). Pitfall #97/98 gate (`scripts/check-inline-iife.mjs`) continues to return 0 broken — confirms the surgical approach is working. The `scripts/templates/ai-bubble.js` scaffold (untracked) is the "Lane C #8 in progress" marker for the next tick.
