# spg-improve-loop · tick 69

📦 PROJECT: stuffprettygood.com
🎯 SUGGESTED: Lane B #6 per-page theme-color — when SPG is saved to the iOS home screen or installed as an Android PWA, the status bar / address bar tint matches the route's brand accent (pink for /stories/, violet for /gift-finder/, cyan for /under-50/, etc.) instead of staying on the global cream/navy. 23 routed pages get brand-tinted pairs from a new PAGE_THEME_COLORS map; 165 product/guide pages fall back to the layout's neutral default (unchanged). Plus 2 stale kanban tickets closed: `t_c9a9a363` (architectural-extract assistantWidget, shipped in f6af2107 tick 63) and `t_60be0328` (compare-mode silent JS error, shipped in febf0818 tick 61 + f6af2107 tick 63).
✅ DONE: scripts/build.mjs (+49/-1: new PAGE_THEME_COLORS map mirroring PAGE_OG_THEMES structure with 23 routes × {light, dark}, and a regex rewrite in mkdirPage that replaces the global cream/navy theme-color triple with the route's brand-tinted pair when PAGE_THEME_COLORS[route] hits); 23 dist/<route>/index.html rebuilt with route-specific theme-color triples (1 line changed per page — still 3 meta tags, different colors); 2 stale kanban tickets closed via hermes kanban --board stuffprettygood-com complete (t_c9a9a363 + t_60be0328 — both blocked-state tickets for work that already shipped in ticks 61 + 63).
🧪 TESTED: `node --check scripts/build.mjs` → 0; `node scripts/validate.mjs` → 155 catalog records OK; `node scripts/build.mjs` → built 155 approved products + 10 guides; `node scripts/check-inline-iife.mjs` → 2607 inline scripts / 0 syntax errors (regex rewrite added to mkdirPage is plain JS, no template-literal pitfalls); offline Python walker across 188 non-/go/ pages → TOTAL: 188, checked: 188, bad: 0 (all 23 routed pages match their expected (light, dark) pair from PAGE_THEME_COLORS; all 165 product/guide pages correctly fall back to the layout default cream/navy because they aren't in the map); live spot check `grep -oE 'meta name="theme-color" content="[^"]+" media="[^"]+"'` on 7 sample routes (home, gift-finder, starter-kits, under-50, walmart, privacy, stories) → all return route-correct pairs (home=f6f1e8/0b1220, gift-finder=fde68a/1e1b4b violet, starter-kits=bae6fd/0c4a6e sky, under-50=fde68a/155e75 cyan, walmart=fde68a/7c2d12 orange, privacy=f3f4f6/111827 neutral gray, stories=fde68a/831843 pink).
📊 RESULTS: commit SHA pending; CF Pages deploy preview ID pending; 25 files staged (`git add -f scripts/build.mjs dist/` per pitfall #52): 23 dist/<route>/index.html + scripts/build.mjs + dist/sitemap.xml (sitemap drift is build-regen side effect per pitfall #84); lane rotation Lane A → Lane B honored (last 2 ticks were Lane A — tick 67 = #23 affiliate, tick 68 = #24 exit-intent).
🔗 LINKS: live preview: https://stuffprettygood.com/ (custom-domain cache-buster `?cb=$(date +%s)`; preview authoritative per pitfall #76 if custom domain lags); report file: `reports/cron-tick-69.md` (this file); commit log: `git log --oneline -3` shows 19d63c21 (tick 68) → 0bd03cdc (tick 67 sitemap) → 7c3606d6 (tick 67 report).
🧠 MEMORY: Per-page theme-color is a 23-route × 2-color extension of the existing Twitter Card injection pattern (tick 44). The mkdirPage signature stays stable — the regex rewrite re-extracts from rendered HTML rather than threading a new opts param through every caller (same pitfall-78 lesson). Next-tick-Hermes can (a) Lane B continue with og:locale:alternate JSON-LD (canonical for international SEO, future-proofs Spanish/French variants), (b) Lane A #25 recently-viewed products bar (uses localStorage so it's offline-safe), or (c) Lane C follow-up on the LLM call shape ticket t_01bc2b18 (blocked on cost/latency decision — needs user greenlight). Two OMNI handoffs are still queued and growing: t_1f986e8b (affiliate-click → Resend post-click ladder) and the new exit-intent signup webhook from tick 68 (no ticket yet but the `spg-signup-exit-intent` event is now firing site-wide and unconsumed).