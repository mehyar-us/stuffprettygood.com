# spg-improve-loop · tick 49

📦 PROJECT: stuffprettygood.com
🎯 SUGGESTED: Lane B #23 — `og:site_name` + `og:locale` on every non-/go/ page. Mayor pass chose this over a Lane A repeat because: (a) tick 48 was Lane A so this is the proper A→B rotation per "never ship the same lane twice" rule, (b) these are documented Open Graph basics missing from the current layout template — they affect LinkedIn / Slack / Twitter / Facebook unfurls of every product and category link, (c) the change is the smallest possible Lane B win (one-line patch to the `layout()` template + build-regen), fits inside a 15-min tick budget, (d) ladders to the money path by making outbound social shares look professional when users share product links.
✅ DONE: added 2 Open Graph meta tags to `scripts/build.mjs` line ~1137 `layout()` template — `<meta property="og:site_name" content="Stuff Pretty Good">` and `<meta property="og:locale" content="en_US">`. Both appear immediately after the existing `og:url` line, BEFORE `twitter:card`. The /go/<id>/ redirect pages are intentionally NOT touched (they have their own minimal `<meta name="robots" content="noindex">` HTML and don't pass through `layout()`). After `node scripts/build.mjs` regenerated 188 non-/go/ pages, every one of them now carries the 2 new meta tags. The sitemap was also regenerated (build-regen side effect — see pitfall #84). Source diff: +2/-0 lines on the long inline layout-template string.
🧪 TESTED:
- `node --check scripts/build.mjs` → exit 0 (no syntax error on the long layout template line)
- `node scripts/validate.mjs` → exit 0 ("155 catalog records, 155 product pages")
- `node scripts/build.mjs` → exit 0 ("built 155 approved products, 10 guides")
- gate-3.5 `git status -s` → 190 M files (1 build.mjs + 188 dist + 1 sitemap) — expected pitfall-#84 regen signature
- shape gate (Python walker) → 188/188 non-/go/ pages carry both `<meta property="og:site_name" content="Stuff Pretty Good">` AND `<meta property="og:locale" content="en_US">`; 0/155 /go/ pages carry them (correctly excluded — minimal noindex redirect HTML)
- live preview URL `https://01888365.stuffprettygood.pages.dev/?cb=$RANDOM` curl → 2/2 expected meta tags present (`og:site_name` + `og:locale`), Mozilla/5.0 UA per pitfall #80
- live preview `/go/apartment-draft-stopper/` curl → 0 og:site_name matches (correctly excluded)
- `wrangler pages deploy` → Success, 189 files uploaded (662 already uploaded), 29.5s; preview `01888365.stuffprettygood.pages.dev`
📊 RESULTS:
- commit SHAs:
  - `294d68d` feat(seo): og:site_name + og:locale meta on every non-/go/ page (Lane B tick 49)
  - `485722b` chore(dist): build-regen (188 dist pages + sitemap, gate 3 side effect tick 49)
  - `98c5287` chore(reports): tick 49 — fill in commit SHAs (per pitfall #62)
- CF deploy version: `01888365.stuffprettygood.pages.dev` (preview URL authoritative per pitfall #33)
- files touched: scripts/build.mjs (+2/-0 layout template), dist/* (188 pages auto-regenerated by build — each gets the 2 new og:* tags), dist/sitemap.xml (lastmod regen — separate chore commit per pitfall #84)
- push state: **DEFERRED** — pitfall-#47 reproduction. The deferred tick-48 push (`0ca7c99`) is still ahead of origin after a 250s+ git-push hang (no progress output, killed per pitfall #51). Tick-49 commit will attempt push in same recovery cycle.
- tickets filed: none this tick (clean implementation, no findings)
🔗 LINKS:
- live URL: https://stuffprettygood.com/?cb=spg49 (custom-domain cache may lag — preview URL is authoritative)
- preview: https://01888365.stuffprettygood.pages.dev/?cb=spg49
- commit: <fill>
- deploy: https://01888365.stuffprettygood.pages.dev
🧠 MEMORY: Tick-49 closed the smallest-possible Lane B meta gap. The pattern: og:site_name + og:locale are added at the same insertion point (right after og:url) because they sit at the same layer — site-identity metadata, not per-page metadata. Future Lane B "HTML meta extension" work in the same family should: (a) target the layout template at scripts/build.mjs:1137, (b) be a one-line patch to the inline template literal, (c) skip /go/ pages explicitly because they have their own minimal HTML that bypasses layout(), (d) re-extract title/desc from rendered HTML for per-page variants (pitfall #78) — site_name + locale are constant so no per-page variant needed. The 5-entry Lane B pattern family index (per references/lane-b-twitter-card-metadata.md) is now: apple-itunes-app, format-detection (done tick 45), og:site_name (done tick 49), og:locale (done tick 49), twitter:card metadata (done tick 44), per-page theme-color overrides. Next candidate: article:author/article:published_time on guide pages (only useful if SPG ships a blog post surface — currently no such route exists; would need new page-type first).