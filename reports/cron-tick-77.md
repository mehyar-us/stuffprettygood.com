# spg-improve-loop · tick 77

📦 PROJECT: stuffprettygood.com
🎯 SUGGESTED: Lane B #15 — per-page `robots` + `googlebot` meta directives. Closes the gap where Google defaults to a thumbnail preview + ~160-char snippet for every SPG page; with `max-image-preview:large + max-snippet:-1 + max-video-preview:-1` Google can show full-resolution preview images + full-text snippets + inline video previews in SERPs. Lane B #14 (per-page RSS auto-discover) was a false-alarm triage: layout() at scripts/build.mjs:1516 already ships the RSS `<link rel="alternate">` site-wide since tick 75 — 188/188 non-/go/ pages confirmed via walker. Ticket t_130006d0 closed false-alarm with evidence comment.

✅ DONE: `scripts/build.mjs` layout() template (line 1516) extended with 2 meta tags after the existing RSS auto-discover link: `<meta name="robots" content="index,follow,max-image-preview:large,max-snippet:-1,max-video-preview:-1">` + matching `<meta name="googlebot">`. Wired into the layout template so all 188 non-/go/ pages pick it up automatically. `/go/<id>/` pages (155) are NOT touched — they keep the legacy `<meta name="robots" content="noindex">` hardcoded inline in the `mkdirPage('go/...')` template (line 1732) since that template doesn't go through layout(). `/open/` (deep-link handler) does go through layout() and gets the new tags — consistent with its sitemap priority (0.3/yearly, same bucket as /signup/ + /unsubscribe/).

🧪 TESTED: Gate 1 `node --check scripts/build.mjs` → exit 0. Gate 2 `node scripts/validate.mjs` → "validation passed: 155 catalog records, 155 product pages". Gate 3 `node scripts/build.mjs` → "built 155 approved products, 10 guides". Gate 3.5 `git status -s` → 188 dist HTML + 2 utility (sitemap+feed) modified (expected, all from layout() regen). Gate 4 `grep -n 'meta name="robots"' scripts/build.mjs` → 1 match (line 1516, the new site-wide injection). Gate 5 offline walker — 188 non-/go/ pages with the new robots+googlebot tag, 0 missing. `/go/` pages: 155 with legacy `noindex`, 0 with new tags. Gate 6 live `curl -A "Mozilla/5.0" https://a4a27932.stuffprettygood.pages.dev/<route>/ | grep robots | googlebot` → 9/9 sampled routes (home, gift-finder, starter-kits, under-50, guides/best-useful-gifts-under-50, products/camping-stove, signup, stories, privacy) all have BOTH tags. `/go/<id>/` live preview confirms legacy `noindex` retained.

📊 RESULTS: 4 commits this tick — chore(04465563) "phase 1 build-verifier side effects" (sitemap+feed lastmod regen) → feat(10fbd541) "Lane B #15 per-page robots + googlebot meta" (1 line in scripts/build.mjs:1516) → chore(c399235d) "Lane B #15 deploy side effects" (sitemap+feed lastmod regen post-build) → chore(b057c05e) "Lane B #15 dist regen" (188 non-/go/ pages). All 4 commits pushed to origin/deploy/legal-expansion-and-signup-modal — verified `git ls-remote origin` matches HEAD `b057c05e`, backlog = 0. wrangler pages deploy ID `a4a27932.stuffprettygood.pages.dev` (190 files uploaded, 663 already uploaded, 17.65s).

🔗 LINKS: preview (authoritative per pitfall #33): https://a4a27932.stuffprettygood.pages.dev/ — verify: `curl -A "Mozilla/5.0" "https://a4a27932.stuffprettygood.pages.dev/" | grep -oE '<meta name="(robots|googlebot)"[^>]*>'` returns both tags. Production: https://stuffprettygood.com/ — cache stale per pitfall #76 (Pages-CDN custom-domain 1-6h refresh). Live verify on /go/apartment-draft-stopper/: legacy `<meta name="robots" content="noindex">` retained, NO new googlebot tag (correct — /go/ must stay de-indexed). Commits: 10fbd541 (feat) + b057c05e (chore-dist) + c399235d (chore-sitemap) + 04465563 (chore-sitemap-phase1). Ticket: t_130006d0 (Lane B #14 RSS auto-discover) closed false-alarm with evidence.

🧠 MEMORY: Lane B pattern family now has 6 entries: site-wide JSON-LD (t42), Twitter Card meta (t44), format-detection (t45), og:site_name+locale (t49), RSS feed (t75), sitemap priority+changefreq (t76), per-page robots+googlebot (t77). Pattern rule for ALL future Lane B meta-extension work: re-extract from rendered HTML rather than threading new params through every caller (pitfall #78 — kept mkdirPage signature stable). All 188/188 non-/go/ pages get the new tags automatically through layout() — no caller signature change. False-alarm triage pattern (pitfall #42) verified again: walker confirmed 188/188 already had RSS auto-discover, no actual bug, ticket closed with evidence. The `mkdirPage('go/...')` template does NOT route through layout() — its inline `<meta name="robots" content="noindex">` is preserved as-is, which is the correct behavior (merchant redirects must stay de-indexed). Next Lane B candidate: per-page `<link rel="canonical">` audit to ensure all 188 pages have a unique canonical URL (already in layout() but worth a walker check) OR `og:see_also` cross-linking between related categories for richer OG cards.