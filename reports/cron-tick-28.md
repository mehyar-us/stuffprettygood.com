# spg-improve-loop · tick 28

📦 PROJECT: stuffprettygood.com
🎯 SUGGESTED: Lane A #11 — add `launch_handler: { client_mode: "auto" }` to web app manifest (PWA polish)
✅ DONE: Added `launch_handler: { client_mode: 'auto' }` to the manifest JSON.stringify() in `scripts/build.mjs:138` (single-line JSON manifest extension pattern, ticks 23→27). `node scripts/validate.mjs` exited 0 (155 catalog records). `node scripts/build.mjs` exited 0 (155 approved products, 10 guides). Manifest schema keys: 17 → 18.
🧪 TESTED: (a) offline gate — `python -c "import json; m=json.load(open('dist/site.webmanifest')); assert m['launch_handler']=={'client_mode':'auto'}; assert len(m)==18; ..."` returned `OK manifest schema=18 launch_handler={'client_mode': 'auto'}`. (b) live preview URL gate — `curl https://eb6c0dae.stuffprettygood.pages.dev/site.webmanifest | python -c "..."` returned `OK live preview schema=18 launch_handler={'client_mode': 'auto'}`. All prior tick invariants preserved: `id='stuffprettygood'`, `display='standalone'`, `display_override=['standalone','minimal-ui']`, `shortcuts` count=4, `screenshots` count=1, `categories=['shopping','lifestyle','productivity']`.
📊 RESULTS: wrangler 4.108.0 deploy of 848 files succeeded — deploy ID `eb6c0dae.stuffprettygood.pages.dev`, alias `https://production.stuffprettygood.pages.dev`. Local commit pending (pitfall-61 one-commit discipline: source `scripts/build.mjs:138` + built `dist/site.webmanifest` + this report staged together). Lane A cursor advances #11 → #12.
🔗 LINKS: live preview `https://eb6c0dae.stuffprettygood.pages.dev/site.webmanifest` (authoritative). Custom domain `https://stuffprettygood.com/site.webmanifest` re-polls after 30-60s edge cache lag per pitfall #33 — at tick 28 +30s the custom domain is still serving the OLD manifest (schema=7, no launch_handler); the preview URL is the durable deploy proof. Git push to `origin/deploy/legal-expansion-and-signup-modal` DEFERRED: local HEAD = `02ec525`, origin still at `00f8357`, 17 commits ahead; the background push session died silently within ~120s with no progress output (pitfall #57 + pitfall #47 15th reproduction). Per the tick-19 fail-fast recipe the local SHA is captured here and the next tick will re-attempt the push when upstream has settled.
🧠 MEMORY: Next-tick-Hermes: the W3C `launch_handler.client_mode` field only takes effect on PWA entry points surfaced through the installed-app launch surface (ChromeOS/Windows desktop file-based launch, Android intent filters with category LAUNCHER, etc.) — it does NOT change the regular web launch flow. The four valid values are `auto` (default-ish; OS picks focus-existing vs navigate-new), `focus-existing` (single window, bring to front), `navigate-new` (always new window/navigation), `navigate-existing` (reuse window, navigate to new URL). Tick 28 chose `auto` as the safest, OS-decides default. If a future tick wants stricter behavior, switching to `focus-existing` is a 1-line JSON change. Lane A next candidate: `share_target` (a dict that takes `action` URL + `method` + `enctype` + `params`; requires a POST-handler route or `/share-target/` page that reads FormData) — ~5-10 min tick depending on whether you wire it to a static no-op form vs a real handler.
