# spg-improve-loop · tick 80

📦 PROJECT: stuffprettygood.com
🎯 SUGGESTED: Lane B #18 — ItemList with full Product shape on 8 category pages. Google can now surface category pages as Product carousels, not bare lists.
✅ DONE: Extended `categoryItemListJsonLd(route, title, picks)` to nest a complete `schema.org/Product` (name + url + image + brand + Offer with priceCurrency/availability) under each `ListItem.item`. Extracted shared `productOfferJsonLd(p)` helper so `productJsonLd` (per-product page) and `categoryItemListJsonLd` (category page) share one Offer/Brand contract. Build covers 8/8 category routes (under-25 / under-50 / home-office / kitchen / pets / tech / travel / useful-finds) with 12 picks each (24 on useful-finds). Walmart excluded per the existing tick-42 gotcha (live-picks-only merchant overlay).
🧪 TESTED: `node --check scripts/build.mjs` exits 0. `node scripts/validate.mjs` reports 155 catalog records, 155 product pages. `node scripts/build.mjs` reports 155 approved products, 10 guides. Offline walker: 8/8 routes OK, every ListItem has full Product shape (full=12/12 on each category, full=24/24 on useful-finds), numberOfItems matches itemListElement length. Live preview gate on `https://4353882d.stuffprettygood.pages.dev`: 8/8 routes OK with the same shape. Product page regression check: `gift-usb-c-charging-station` still emits complete Product shape with brand + Offer + image + description. Walmart verified to NOT emit an ItemList (gotcha preserved).
📊 RESULTS: commit `4adac857` — `feat(seo): Lane B #18 ItemList with full Product shape on 8 category pages`. 166 files changed (+409/-378). Push landed clean via `GIT_CONFIG_SYSTEM=/dev/null` (pitfall #96 verified): origin SHA `4adac8577c565ac32ae37859bf93c68f009e52e1` matches HEAD, 0 commits behind. CF Pages deploy URL: `https://4353882d.stuffprettygood.pages.dev` (preview authoritative; custom domain cache stale per pitfall #33 — will refresh on its own 1-6h schedule). Production alias `https://production.stuffprettygood.pages.dev` mirrors the deploy. Ticket `t_844fe732` (Lane B #18 candidate: ItemList with full Product shape on category pages) — closed in this tick.
🔗 LINKS: Live preview (canonical until custom domain refreshes): https://4353882d.stuffprettygood.pages.dev/home-office/ · https://4353882d.stuffprettygood.pages.dev/under-50/ · https://4353882d.stuffprettygood.pages.dev/useful-finds/ · GitHub: https://github.com/mehyar-us/stuffprettygood.com/commit/4adac857
🧠 MEMORY: Lane B ItemList win pattern — nest full Product shape under ListItem.item rather than top-level url/name. Centralize the Offer/Brand contract in one helper so both `productJsonLd` and `categoryItemListJsonLd` can share it. The schema validates end-to-end (8/8 routes, full shape on every ListItem) and the Offer block on the per-product page still renders identically (regression test passed on a sample product). Walmart's gotcha (no static picks → no ItemList) is preserved by the `!isWalmart && picks.length` guard at scripts/build.mjs:1880 and should not be changed. Next Lane B candidate after this: ItemList on individual product pages that have siblings (e.g. category-filtered landing pages already covered), OR extend `image` to include multiple `image` URLs (currently single — `array of image URLs` is the Google-recommended shape for Product rich results with multiple photos). Filed as next-tick candidate below.