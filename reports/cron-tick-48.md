# spg-improve-loop · tick 48

📦 PROJECT: stuffprettygood.com
🎯 SUGGESTED: Lane A #22 (saved picks) + lane-D false-alarm triage. Mayor pass chose saved picks over another Lane B meta extension because: it ladders to the email-capture money line (saved-list → "Email me this list" → /signup/), and it builds on the verified compare-mode capture-phase IIFE pattern. Tick 47 was Lane B so this is the proper A→B rotation per "never ship the same lane twice" rule.
✅ DONE: added 8th AI-bubble bind* helper — `bindSavedPicks()` IIFE. Every `.ai-pick` card now ships with an inline star toggle (`ai-save-btn` SVG, position-absolute top-right, 30×30 touch target). State persists to `localStorage['spg-saved-picks:v1']` (max 24 picks, FIFO eviction). The AI-launch FAB carries an `ai-launch-pill` counter badge that hides at 0 and shows the count. `FOLLOW_UPS_BY_KIND.saved` added — "Email me this list" routes the user to /signup/. `answer()`'s picks branch now embeds the star button inline; new `/saved|saved picks|my picks|wishlist|saved list|show saved/` query branch reads the same key and renders the list back as a 'saved' answer kind. Capture-phase click delegation (preventDefault + stopImmediatePropagation) so the star never accidentally fires the compare-mode /products/<id>/ click. MutationObserver on `.ai-messages` refreshes star visual state as new bot answers land. CSS adds `.ai-save-btn`, `.ai-save-btn.active`, `.ai-launch-pill`, `@keyframes ai-save-pop`, and `.ai-bubble.compare-on .ai-save-btn` (disabled while compare mode is on, opacity .35) — kept inside the existing 2-space style convention.
🧪 TESTED:
- `node scripts/validate.mjs` → exit 0 ("155 catalog records, 155 product pages")
- `node scripts/build.mjs` → exit 0 ("built 155 approved products, 10 guides")
- gate-3.5 `git status -s` → 192 M files (190 dist + sitemap + scripts + src) — exactly the pitfall-#84 regen signature (sitemap lastmod forward-tick)
- shape gate (Python walker) → 18/18 source checks pass; 188/188 non-/go/ pages carry the pill markup + save-btn class
- extracted SAVED_KEY block → `node --check _tmp.mjs` exits 0 (per pitfall #69)
- `wrangler pages deploy` → Success, 190 files uploaded (661 already uploaded), 20.98s
- preview URL `https://9cdcdf35.stuffprettygood.pages.dev` curl: 8/8 expected markers present (data-ai-launch-pill, ai-save-btn, SAVED_KEY, saved-picks:v1, function loadSaved/toggleSave/refreshStarState, saved branch matchers)
- browser QA on `?cb=spg48qa`: panel renders, pill_present=true with pill_hidden=true when empty (correct initial state), 4 suggestion chips present, ai_bubble_present=true
📊 RESULTS:
- commit SHA: <fill after `git rev-parse HEAD`>
- CF deploy version: `9cdcdf35.stuffprettygood.pages.dev` (preview URL authoritative per pitfall #33)
- files touched: scripts/build.mjs (+90/-2 IIFE add + picks branch star button + saved branch + FOLLOW_UPS kind), src/styles.css (+22/-1 CSS), dist/* (188 pages auto-regenerated by build), dist/sitemap.xml (lastmod regen — separate chore commit per pitfall #84)
- tickets filed: t_48omni (OMNI: wire Resend + email-me-this-list webhook — money path critical, per mayor trigger table), t_48falsealarm (commented on existing t_56b11ab9 with pitfall #42 evidence that the live page already conforms — pwaRegistration IIFE on line 238 only injects the button when `beforeinstallprompt` fires; no static markup ships)
🔗 LINKS:
- live URL: https://stuffprettygood.com/?cb=spg48 (custom-domain cache may lag — preview URL is authoritative)
- preview: https://9cdcdf35.stuffprettygood.pages.dev/?cb=spg48
- commit: <fill>
- deploy: https://9cdcdf35.stuffprettygood.pages.dev
- pitfall-#42 triage: https://hermes/cli → `hermes kanban --board stuffprettygood-com show t_56b11ab9`
- OMNI ticket: `hermes kanban --board stuffprettygood-com show t_48omni`
🧠 MEMORY: Tick-48 dropped the first lane-A IIFE that ladders to the email-capture money line. The `bindSavedPicks()` pattern is small (90 lines) but opens a new door: once a user has 3+ saved picks, the FOLLOW_UPS "Email me this list" chip drives them to /signup/, which feeds the Resend webhook path that OMNI is going to wire next tick. Future tick-49 candidate: ship the follow-up "Email me my list" → actually-fire-the-resend-audience webhook path (cross-team — file OMNI ticket + a backend ticket for the POST handler). The pattern family for lane-A-LocalStorage IIFEs is now: `bindVoice` (feature-detect), `bindVerdict` (feature-gate), `bindShare` (clipboard + URL hash), `bindCompare` (capture-phase delegation + state machine), `bindSavedPicks` (capture-phase delegation + persistent state + counter pill). All five share the `function XXX(){}` hoisting-after-call-site pattern (pitfall-#41), all use the same IIFE structure, all read prefs via try/catch, all expose a feature-gated button with `aria-pressed` mirroring state.
