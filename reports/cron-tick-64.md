# spg-improve-loop · tick 64

📦 PROJECT: stuffprettygood.com
🎯 SUGGESTED: **Lane C follow-up: kill the LAST 2 `new RegExp()` calls in `scripts/build.mjs`** — the surgical-regex-rewrite work that the tick-63 architectural extraction (assistantWidget → `scripts/templates/ai-bubble.js`) made possible. Both remaining calls were inside `mkdirPage()`'s template-literal layer, where string interpolation consumes regex backslashes silently (pitfall #97 / #98). **Replacing them with plain JS `.indexOf()` / `.split()` / `.replace(/literal/, '')` patterns eliminates the corruption class for those code paths too — `new RegExp()` count in build.mjs goes 2 → 0.**

✅ DONE: surgical rewrite of 2 remaining `new RegExp()` sites in `scripts/build.mjs`. **(1) Cookie regex (line 605, inside the rate-limit script template):** was `document.cookie.match(new RegExp('(?:^|; )' + CONFIG.cookieName + '=([^;]*)'))` — replaced with a `document.cookie.split(';')` + `for` loop that strips leading whitespace with a static `/^\s+/` literal and tests `c.indexOf(prefix) === 0`. No dynamic regex construction. **(2) Aria-current nav-link regex (line 1247, inside `mkdirPage()`):** was `new RegExp(\`(<div class="nav-links">[^<]*(?:<a[^>]*>[^<]*</a>[^<]*)*?)<a href="${targetHref.replace(/\//g, '\\/')}">\`)` — replaced with a 4-call `indexOf()` + `slice()` chain that locates the `<div class="nav-links">` block, finds the matching `<a href="/<route>/">` anchor inside it, and splices in `aria-current="page"`. No regex at all — pure string indexing. **Source diff:** `scripts/build.mjs` +21/-6 (1 function signature change for the cookie one, 1 block replacement for the aria-current one). Both changes preserve behavior exactly: cookie lookup still returns the existing ID, aria-current still marks the right nav link on the 6 nav routes + home logo. `grep -c "new RegExp(" scripts/build.mjs` = **0** (was 2 at tick 63, 7 at tick 62, 5 at tick 61).

🧪 TESTED: gate 1 `node --check scripts/build.mjs` → exit 0; gate 2 `node scripts/validate.mjs` → "validation passed: 155 catalog records, 155 product pages"; gate 3 `node scripts/build.mjs` → "built 155 approved products, 10 guides" (regenerated 188 dist HTML pages + 1 sitemap + 1 ai-bubble.js); gate 4 `node scripts/check-inline-iife.mjs` → "checked 2419 inline script bodies across 188 dist pages / OK: all inline script bodies parse cleanly"; gate 5 `grep -nE "new RegExp\(" scripts/build.mjs` → **0 matches** (only a comment in the aria-current code mentions the word); gate 6 offline dist walker — `document.cookie.match(new RegExp` appears in 0/188 dist pages (was 188/188 before this fix); gate 7 offline route auditor — all 23 routes have correct aria-current placement (home logo for `/`, the 6 nav routes' links for `/gift-finder/`, `/starter-kits/`, `/under-50/`, `/walmart/`, `/stories/`, `/signup/`; non-nav routes correctly have no aria-current on the nav); gate 8 live preview `curl -A "Mozilla/5.0" https://ca003e05.stuffprettygood.pages.dev/?cb=verif64` → logo `aria-current="page"`, no old cookie regex in inline scripts; gate 9 live preview `curl -A "Mozilla/5.0" /gift-finder/` → `aria-current="page" href="/gift-finder/"` present; gate 10 live preview `/signup/` → `aria-current="page" href="/signup/"` present; gate 11 live preview `/scripts/templates/ai-bubble.js` → 39575 bytes, `_PAGE_SUGGESTIONS` baked in; gate 12 JSON-LD site-wide Organization still intact.

📊 RESULTS: commit `e906f81b` (190 files / +1352 / -197); push `9b9bf40a..e906f81b` (verified via `git ls-remote` SHA match + `git rev-list --count origin..HEAD` = 0); wrangler deploy ID `ca003e05.stuffprettygood.pages.dev` (189 files uploaded in 13.88s, 663 already cached, 852 total). **Tickets filed:** none new — the parent ticket `t_c9a9a363` ("EXT-architectural-extract-assistantWidget: resolve pitfall #97 / #98 template-literal regex corruption class") is now ALSO fully resolved for the 2 remaining inline regexes in `mkdirPage()` that survived tick 63. The ticket can be closed.

🔗 LINKS: live preview (authoritative): https://ca003e05.stuffprettygood.pages.dev/ ; ai-bubble.js direct: https://ca003e05.stuffprettygood.pages.dev/scripts/templates/ai-bubble.js ; production custom domain (cache-stale, will refresh per pitfall #72): https://stuffprettygood.com/ ; commit: https://github.com/mehyar-us/stuffprettygood.com/commit/e906f81b ; deploy alias: https://production.stuffprettygood.pages.dev .

🧠 MEMORY: **`new RegExp()` count in `scripts/build.mjs` is now ZERO.** The full pitfall #97 / #98 corruption class (template-literal backslash consumption silently breaking regex patterns at runtime) is eliminated across all 4 sites that ever had the bug: (1) compare-mode click delegate, (2) share-URL pick extraction, (3) assistantWidget()'s ~100 internal regexes (extracted to `scripts/templates/ai-bubble.js` tick 63), and (4) the 2 remaining inline ones in cookie + aria-current (this tick). **Future Lane C / mkdirPage work:** any new code that needs pattern matching should use (a) plain `/regex/` literals, (b) `String.prototype.split` + `indexOf`, or (c) move into `scripts/templates/ai-bubble.js` as plain JS. NEVER use `new RegExp(\`...${variable}...\`)` inside the layout() template literal — it WILL silently break under backslash consumption. The `scripts/check-inline-iife.mjs` gate remains the primary regression detector; this change makes it even cleaner (the 188 inline scripts that previously contained the cookie regex now contain the split/loop version, which is a pure data-flow change with no regex backslashes in the source text). **Lane C #9 candidate (next tick):** add the real LLM call shape — `t_01bc2b18` research card is closed, the bubble is now in its own file with clean regex semantics, so wiring in `fetch('/api/companion', {method:'POST', body:...})` is the natural next step. But that's a multi-tick workstream that should file fresh tickets first; not a single-tick lane.