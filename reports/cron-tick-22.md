# spg-improve-loop · tick 22

📦 PROJECT: stuffprettygood.com
🎯 SUGGESTED: Lane A #6 — offline indicator for installed PWA
✅ DONE: Pull-to-refresh's sister feature. `offlineIndicatorScript()` IIFE wired into layout() between `pullToRefreshScript()` and `modalHtml`. Same gate pattern as tick 21: hard early-return unless `html.is-pwa`, lazy DOM creation in `ensureBanner()` triggered by FIRST `window` `offline` event. Banner is a fixed-top slide-down with amber→orange gradient matching the PTR "is-ready" palette. pointer-events:none so it cannot intercept taps on the nav. aria-live=polite so AT users hear the transition without it yelling. SW_VERSION bumped from `spg-v1` to `spg-v2` so the new SW evicts old caches and the new offline indicator actually has offline state to detect.
🧪 TESTED: `node scripts/validate.mjs` exit 0 (155 catalog / 155 products). `node scripts/build.mjs` exit 0. `curl preview | grep -oE '(spg-offline|offlineIndicatorScript|navigator.onLine|addEventListener..offline|addEventListener..online|spg-offline-dot|is-visible)' | sort | uniq -c` returns 1+1+1+1+1+2+1 = all 7 expected markers in served HTML on https://54b518c3.stuffprettygood.pages.dev/. Browser QA: regular tab → `is-pwa:false`, banner absent (IIFE early-returned). Simulated `is-pwa` mode + manual `.is-visible` toggle → transform animates from `translate(-50%,-100%)` to `translate(-50%,0)` over 280ms cubic-bezier, settle confirmed via 400ms wait → banner ends at `transform: matrix(1, 0, 0, 1, -36.7266, 0)` (on-screen). z-index 9050 confirmed (below PTR 9100, above splash 9999 boot-only). pointer-events:none confirmed. backgroundImage amber→orange gradient confirmed. dot element + label render correctly. Class swap round-trip clean: add→visible, remove→hidden (settled). 3 pre-existing empty-message JS errors in console (per dogfood skill note: thrown by PREVIOUS scripts, message field is empty but they don't break our feature — our IIFE early-returns before the SW throw site, so our handlers are unaffected).
📊 RESULTS: scripts/build.mjs +57/-1, src/styles.css +18/-0, src/sw.js +1/-1, 189 dist files (188 regenerated by build + dist/sw.js bumped to v2). Wrangler deploy `54b518c3` (production branch). All offline markers verified on preview URL.
🔗 LINKS:
- Production: https://stuffprettygood.com/ (edge cache may lag 30-60s; preview URL is authoritative until then — pitfall #33)
- Preview (deploy verification): https://54b518c3.stuffprettygood.pages.dev/
- Live SW v2: https://54b518c3.stuffprettygood.pages.dev/sw.js (`const SW_VERSION = 'spg-v2'`)
- Commit: edc71de (local HEAD; push to origin in flight, 8 commits ahead of remote HEAD 00f8357, retry next tick per pitfall #47 slow-upstream protocol)
- git push: in flight at tick close (background process proc_f8f7eaea3fb2 still alive; multiple git worker procs visible in ps); will retry next tick with verify recipe. Production deploy via wrangler is the durable user-facing ship proof.
🧠 MEMORY: Pitfall #54 (tick 21) confirmed — `write_file` with absolute paths is required for this Windows env. Pitfall #50 re-checked: patch on `function offlineIndicatorScript()` (a fresh block, no surrounding indent context) worked clean on first try — single ~50-line new_string at top-level function indent. NEW LAYER: the `.spg-offline` transform animation needs ≥400ms to settle before reading computedStyle in browser QA — earlier reads return mid-flight matrices. Future PWA-only feature recipe (per `lane-a-pull-to-refresh.md`): always use `is-pwa` gate + `ensure*()` lazy DOM + `pointer-events:none` + z-index ≤ 9100 (above splash during boot, below PTR).